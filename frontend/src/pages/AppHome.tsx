/** Placeholder — the real app shell lands later in this PR (issue #11). */
function AppHome() {
  return (
    <main className="flex min-h-dvh items-center justify-center px-6">
      <p className="font-mono text-sm text-fg-muted">The app lives here soon.</p>
    </main>
  )
}

export default AppHome
