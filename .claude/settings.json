{
  "showClearContextOnPlanAccept": true,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Formatting backend Python…",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/backend/*.py) (cd \"$CLAUDE_PROJECT_DIR/backend\" && uv run ruff format \"$f\" && uv run ruff check --fix \"$f\") ;; esac; } || true"
          }
        ]
      }
    ]
  }
}
