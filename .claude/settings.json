{
  "showClearContextOnPlanAccept": true,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Formatting backend Python…",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *\"/backend/\"*.py) (cd \"$CLAUDE_PROJECT_DIR/backend\" && uv run ruff format \"$f\" && uv run ruff check --fix \"$f\") ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
