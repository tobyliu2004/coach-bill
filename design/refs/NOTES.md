# Reference notes

One line per reference: the filename, and what Toby likes about it. The images
next to this file are gitignored (public repo, other people's screenshots), so
**this text is the only part that survives** — write the line as if the image is
already gone.

Claude reads this before proposing anything visual, and turns each line into a
brief per step 2 of `../how-we-design.md`: the *mechanism*, not the adjective.

---

## References

- **meuze.ai** — the standing reference class. Scroll is the medium, not a
  side-effect: a pinned stage where scroll progress scrubs a real sequence,
  type that arrives by unmasking rather than fading, and one enormous quiet
  element carrying the page. Already spent on: `DataAthlete` and
  `CheckInChapter`.

<!-- Add references here. Format:
- **<filename>** — what it does that you want. Mechanism if you can name it.
-->

---

## Hero critiques

Toby's outstanding objections to the landing hero — **still unextracted**.
They live in his head, which is exactly the failure this protocol exists to
fix. Filling this in is the protocol's first real use (issue #28), and it is
its own session: refs in hand, brief first, acceptance checks agreed, and only
then a build.

<!-- One line per critique. What's wrong, and — if you can — what it should do
     instead. "The X does Y; it should do Z" beats "the X feels off". -->
